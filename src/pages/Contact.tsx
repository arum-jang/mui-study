import { Box, Typography } from '@mui/material';

const Contact = () => {
  return (
    <Box sx={{ padding: 2 , background : 'red' }}>
      <Typography variant="h4">
        문의해라 무니해
      </Typography>
    </Box>
  );
};

export default Contact;